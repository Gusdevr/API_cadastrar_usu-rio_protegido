# API Cadastrar Usuário Protegido

API feita com Node.js, Express, Sequelize e JWT, usando banco de dados MySQL hospedado no Railway.

## Rodar Localmente

Crie um arquivo `.env` baseado no `.env.example`.

Instale as dependências:

```bash
npm install
